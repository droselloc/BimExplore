<Window x:Class="BimExplorer.App.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Configuracion" Height="520" Width="720"
        WindowStartupLocation="CenterOwner"
        Background="#1E1E1E" ShowInTaskbar="False">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="CONFIGURACION" FontWeight="SemiBold"
                   Foreground="White" FontSize="18" Margin="0,0,0,10"/>

        <TextBlock Grid.Row="1" Text="Carpetas indexadas"
                   Foreground="#888" FontSize="11" Margin="0,6,0,8"/>

        <Border Grid.Row="2" Background="#252526" CornerRadius="4" BorderBrush="#3F3F46" BorderThickness="1">
            <ListBox x:Name="FoldersList" ItemsSource="{Binding Folders}"
                     Background="Transparent" BorderThickness="0"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Padding" Value="0"/>
                        <Setter Property="Margin" Value="0"/>
                    </Style>
                </ListBox.ItemContainerStyle>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border Background="#2D2D30" Padding="10,8" Margin="6,4" CornerRadius="4">
                            <DockPanel>
                                <Button DockPanel.Dock="Right" Content="Dejar de indexar"
                                        Command="{Binding DataContext.RemoveFolderCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                        CommandParameter="{Binding}"
                                        Background="#C24B3A" Foreground="White" BorderThickness="0"
                                        Padding="10,6" FontSize="11" Cursor="Hand"/>
                                <StackPanel>
                                    <TextBlock Text="{Binding Path}" Foreground="#DDD" FontSize="12"
                                               TextTrimming="CharacterEllipsis"/>
                                    <TextBlock Foreground="#888" FontSize="10" Margin="0,2,0,0">
                                        <Run Text="Agregada: "/>
                                        <Run Text="{Binding AddedAtUtc, StringFormat='{}{0:yyyy-MM-dd HH:mm}', Mode=OneWay}"/>
                                        <Run Text="  ·  Ultima indexacion: "/>
                                        <Run Text="{Binding LastIndexedAtUtc, StringFormat='{}{0:yyyy-MM-dd HH:mm}', TargetNullValue='—', Mode=OneWay}"/>
                                    </TextBlock>
                                </StackPanel>
                            </DockPanel>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Border>

        <TextBlock Grid.Row="3" Text="{Binding StatusText}" Foreground="#888"
                   FontSize="11" Margin="2,8,0,0"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="Cerrar" Width="100" Padding="0,8"
                    Background="#0E639C" Foreground="White" BorderThickness="0"
                    Cursor="Hand" Click="Close_Click" IsDefault="True"/>
        </StackPanel>
    </Grid>
</Window>
